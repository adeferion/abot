#include <imgui.h>
#include <iostream>

using namespace std;

namespace uselessShit {
    extern ImFont* fontBigger;

    bool stringToBool(string s);
    string boolToString(bool b);
    int random_range(int from, int to);
}